<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        PERMISOS NFC - Descuento Estudiante
        
        Permite a la app leer tags NFC para validar tarjetas estudiantiles
        y aplicar descuento del 10% en el carrito de compras.
    -->
    <uses-permission android:name="android.permission.NFC" />

    <!--
        FEATURE NFC (OPCIONAL)
        
        android:required="false" permite que la app se instale y funcione
        en dispositivos sin hardware NFC. La funcionalidad de descuento
        simplemente no estará disponible en esos dispositivos.
        
        Si se cambia a "true", la app solo aparecerá en Play Store
        para dispositivos con NFC.
    -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_huerto"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_huerto"
        android:supportsRtl="true"
        android:theme="@style/Theme.Huerto_Hogar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Huerto_Hogar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>